// Local/dev placeholder. Vercel's build command overwrites this file with
// the real backend URL from the VITE_API_BASE_URL env var before deploying
// (see docs/deployment-plan.md). Left as a no-op here so app.js's own
// "http://localhost:8000" fallback applies for local testing.
